  <nav aria-label="Page navigation example">
    <p style="position: absolute;"><%= results.pagination.pageSize %> por página</p>
    <ul class="pagination" style="display: inline-flex;">
      <li class="page-item">
      <% if(results.pagination.currentPage>0) { %> 
        <a class="page-link" href="/produtos?page=<%= results.pagination.currentPage-1 %>&pageSize=<%= results.pagination.pageSize %>">Anterior</a>
      <% } %>
      </li>
      <% for(let i = 0; i<results.pagination.pages; i++) { %> 
      <li class="page-item <% if(i==results.pagination.currentPage) { %> active <% } %>">
        <a class="page-link" href="/produtos?page=<%= i %>=&pageSize=<%= results.pagination.pageSize %>"> <%= i+1 %> </a>
      </li>
      <% } %>
      <li class="page-item">
      <% if(results.pagination.currentPage < results.pagination.pages -1) { %> 
        <a class="page-link" href="/produtos?page=<%= results.pagination.currentPage+1 %>&pageSize=<%= results.pagination.pageSize %>">Proximo</a>
      <% } %>
      </li>
    </ul>
  </nav>